{
  "name": "drivebuddy-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && cp index.html robots.txt sitemap.xml _redirects dist/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {}
}
